# specify path to iote.conf or leave blank
# default is the same as IoTECore
#dash_conf=/home/evan82/.iotedata/iote.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
